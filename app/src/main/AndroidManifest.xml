<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noorulain.second" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.CAMERA" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application android:hardwareAccelerated="true" android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Rex"
            android:label="face recognition"/>

        <!-- <activity android:name=".InGenreSongs"
            android:label="GENRE"/>

        <activity android:name=".InAlbumSongs"
            android:label="ALBUM"/>

        <activity android:name=".InArtistAlbum"
            android:label="ARTIST"/>

        <activity android:name=".InPlaylistSongs"
            android:label="PLAYLIST"/>-->

        <activity android:name=".PlaylistCreator"
            android:label="PLAYLIST CREATOR"
            android:theme="@android:style/Theme.NoDisplay"/>


        <service android:name="com.example.noorulain.second.MusicService" />

    </application>

</manifest>
